node_modules/
api/client/
api/.data/
data/
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
.DS_Store
.env
.claude/settings.local.json

# Deployment. This is a public repo, so the auth key, node identity and tailnet name
# stay untracked.
# deploy/serve.json is generated from serve.json.template by deploy/sidecar.sh.
deploy/.env
deploy/serve.json
deploy/tailscale_state/
